# macos-enterprise-sso-bridge

## Agent skills

### Issue tracker

Issues live in this repo's GitHub Issues (`MaxAnderson95/macos-enterprise-sso-bridge`), operated through the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

The five canonical triage labels are used unchanged (`needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`). See `docs/agents/triage-labels.md`.

### Domain docs

Single-context: `CONTEXT.md` at the repo root and ADRs under `docs/adr/`. See `docs/agents/domain.md`.
